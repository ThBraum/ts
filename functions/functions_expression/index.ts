//Functions expression

/* Uma função expression pode ser atribuída a uma variável, pode ser passada como um argumento para outra função, pode ser retornada de outra função...
Após uma função expression ter sido armazenada em uma variável, a variável pode ser usada como uma função. 
Funções armazenadas em variáveis ​​não precisam de nomes de funções. Eles são sempre invocados (chamados) usando o nome da variável. 

Uma expressão de função pode ser usada como uma IIFE (Immediately Invoked Function Expression) que é executada assim que é definida.*/

/* const add = function (a: number, b: number): number {
    return a + b;
}

const sub = function (a: number, b: number): number {
        return a - b;
}

const mult = function (a: number, b: number): number {
    return a * b;
}

const div = function (a: number, b: number): number {
    return a / b;
} */