//Funções

/* As funções declaration são carregadas antes de qualquer código ser executado, 
enquanto as funções expression são carregadas apenas quando o interpretador atinge essa linha de código.

//Parâmetros opcionais
function greet(name: string, lastName?: string) {
    console.log('Olá ' + name + ' ' + lastName);
}

greet('Braum');
greet('Thomaz', 'Braum'); 

//Parâmetros default
function greet(name: string, lastName: string = 'Braum') {
    console.log('Hello ' + name + ' ' + lastName);
}

greet('Thomaz'); 

//Parâmetros rest
function sum(...numbers: number[]) {
    return numbers.reduce((a, b) => a + b);
}

console.log(sum(1, 2, 3, 4, 5)); */

