//Getters & setters

/* Permitem controlar o acesso às propriedades de uma classe - definindo regras de acesso e validação. 
getter => Este método vem quando você deseja acessar qualquer propriedade de um objeto. Um getter também é chamado de accessor.
setter=>  Este método vem quando você deseja alterar qualquer propriedade de um objeto. Um setter também é conhecido como um mutator.
*/

/* class Pessoa {
    private _idade: number = 0;

    get idade(): number {
        return this._idade;
    }

    set idade(valor: number) {
        if (valor >= 0 && valor <= 120) {
            this._idade = valor;
        }
    }
}

const pessoa = new Pessoa();
pessoa.idade = 10;
console.log(pessoa.idade); */
